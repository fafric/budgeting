<%- include('partials/header', { title: 'Income' }) %>

<div class="row g-4">
  <div class="col-lg-4">
    <div class="card shadow-sm">
      <div class="card-body">
        <h1 class="h4 mb-3">Add income</h1>
        <% if (error) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <form method="POST" action="/income" class="d-grid gap-3">
          <input type="text" name="source" class="form-control" placeholder="Source (Salary, Freelance, etc.)" required>
          <input type="text" name="category" class="form-control" placeholder="Category" required>
          <input type="number" step="0.01" min="0.01" name="amount" class="form-control" placeholder="Amount" required>
          <input type="date" name="date" class="form-control" value="<%= today %>" required>
          <textarea name="note" class="form-control" rows="3" placeholder="Optional note"></textarea>
          <button type="submit" class="btn btn-dark">Save income</button>
        </form>
      </div>
    </div>
  </div>

  <div class="col-lg-8">
    <div class="card shadow-sm">
      <div class="card-body">
        <h2 class="h4 mb-3">Income history</h2>
        <div class="table-responsive">
          <table class="table align-middle">
            <thead>
              <tr>
                <th>Date</th>
                <th>Source</th>
                <th>Category</th>
                <th>Amount</th>
                <th>Note</th>
                <th></th>
              </tr>
            </thead>
            <tbody>
              <% if (!items.length) { %>
                <tr><td colspan="6" class="text-muted">No income records yet.</td></tr>
              <% } %>
              <% items.forEach(item => { %>
                <tr>
                  <td><%= item.date %></td>
                  <td><%= item.source %></td>
                  <td><%= item.category %></td>
                  <td class="text-success fw-semibold"><%= formatMoney(item.amount) %></td>
                  <td><%= item.note %></td>
                  <td>
                    <form method="POST" action="/income/<%= item.id %>/delete">
                      <button class="btn btn-sm btn-outline-danger">Delete</button>
                    </form>
                  </td>
                </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
